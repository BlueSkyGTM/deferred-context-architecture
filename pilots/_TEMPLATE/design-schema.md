# design-schema.md — TEMPLATE (a STUB by design; cut at schema-discovery, never pre-filled)

This file declares the schema's path — the slot core names
(`stages/04-iteration/CONTRACT.md` → design-schema bootstrap). The schema itself is CUT during
schema-discovery iteration 1 from logs of proven-good work; it is earned, not given. Do not fill it
by hand before the run.

## What the cut schema will govern (shapes for a Claude Code system)
When schema-discovery writes this file, it captures the SHAPE quality lives in, typically:
- the skill-file schema + description/trigger conventions (per the current skill schema and
  skill-creator conventions);
- the agent definition shape;
- the governed CLAUDE.md's structure;
- hook config validity (settings.json entries);
- MCP config shape (`.mcp.json`).

Point to the authoritative external schemas; do not restate copies that will go stale.

## Status
`<empty — awaiting schema-discovery iteration 1>`
